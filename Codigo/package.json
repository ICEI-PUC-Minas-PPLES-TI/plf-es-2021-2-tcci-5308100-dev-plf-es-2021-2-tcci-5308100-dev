{
  "name": "sec",
  "version": "1.0.0",
  "license": "ISC",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build:common": "yarn workspace @sec/common build",
    "build:api": "yarn workspace @sec/server build",
    "build:app-web": "yarn workspace @sec/app-web build",
    "start:api": "yarn workspace @sec/server start",
    "start:app-web": "yarn workspace @sec/app-web start"
  },
  "dependencies": {
    "yup": "^0.32.11"
  },
  "devDependencies": {
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}